# The main pipeline for dab_data_engineering

resources:
  pipelines:
    sunny_bay_roastery:
      catalog: ${var.catalog}
      serverless: true
      root_path: "."

      libraries:
        - glob:
            include: transformations/**
